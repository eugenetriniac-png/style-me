# Pick one provider
TRYON_PROVIDER=fal          # or: replicate

# fal.ai
FAL_KEY=

# Replicate
REPLICATE_API_TOKEN=
REPLICATE_VERSION=          # the pinned version hash of the try-on model

# Optional: stylist copy from a real model
ANTHROPIC_API_KEY=

# Lock this down once the site has a real domain
ALLOWED_ORIGIN=*
